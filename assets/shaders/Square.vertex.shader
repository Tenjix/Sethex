// shadertype=glsl
#version 330

// generic vertex shader to use in combination with the generic geometry shader to produce a unit square geometry input for a simulation shader
// used by sending a single point: glDrawArrays(GL_POINTS, 0, 1);

void main() {}
